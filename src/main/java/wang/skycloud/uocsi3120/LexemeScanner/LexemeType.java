package wang.skycloud.uocsi3120.LexemeScanner;

/**
 * LexemeType is an enum class that defines the types of lexemes.
 */
public enum LexemeType {
    literal,commentLine,commentBlock,String,EOF,operator,semicolon,separator
}
